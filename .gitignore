# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Data (raw replays are large and can be re-downloaded from HuggingFace)
data/raw/*.json
data/raw/*.json.lz4
data/raw/*.tar.gz
# Processed battles are tracked for reproducibility (10K battles, ~300MB compressed)
# data/processed/battles/*.npz  -- tracked in git
data/processed/*.parquet
data/processed/*.bin

# Checkpoints (large model files - baseline checkpoints tracked)
# checkpoints/baseline_mlp/ and checkpoints/baseline_gru/ are tracked
checkpoints/transformer/
checkpoints/*.ckpt

# Experiment tracking
wandb/
mlruns/

# OS
.DS_Store
Thumbs.db

# Environment variables
.env
